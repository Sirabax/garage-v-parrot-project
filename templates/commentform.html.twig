{{ form_start(form) }}
    <div class="form-group">
        {{ form_row(form.name) }}
    </div>
    <div class="form-group">
        {{ form_row(form.rating) }}
    </div>
    <div class="form-group">
        {{ form_row(form.comment) }}
    </div>
    {{ form_row(form.submit) }}
{{ form_end(form) }}